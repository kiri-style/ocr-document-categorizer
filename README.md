# OCR Document Categorizer

This application detects a document inside an image, extracts text using OCR,
and categorizes the content into sections.

## How to run
```bash
pip install -r requirements.txt
streamlit run app.py